apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # change the image value below to your controller image URL
      - image: "pierro777/cluster-api-provider-exoscale:latest"
        name: manager
